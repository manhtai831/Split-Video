services:
  split_video_service:
    build: .
    restart: always
    container_name: split_video_service
    env_file:
      - .env
    volumes:
      - ./:/app
      - ./data/database:/app/database
      - ./data/uploads:/app/uploads
      - ./data/logs:/app/logs
    ports:
      - 8002:3000
      - 2345:2345

    cap_add:
      - SYS_PTRACE

    security_opt:
      - seccomp:unconfined
